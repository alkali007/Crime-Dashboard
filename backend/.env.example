# Backend Environment Variables (Local Development)
# Rename this file to .env locally. DO NOT commit .env to GitHub.

# Database Connection
# Replace with your local Postgres connection or superset URI
DB_DSN="host=localhost user=postgres password=postgres dbname=crime_dashboard port=5432 sslmode=disable"

# Server Port
PORT=8080
