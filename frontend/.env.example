# Frontend Environment Variables (Local Development)
# Rename this file to .env.local locally. DO NOT commit .env.local to GitHub.

# API URL
# Points to the Go backend. For local dev, use localhost.
# For production (Vercel), set this in the Vercel Dashboard.
NEXT_PUBLIC_API_URL=http://localhost:8080
